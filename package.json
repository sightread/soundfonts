{
  "name": "@sightread/soundfonts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Reproducible packaging and release tooling for Sightread soundfonts",
  "scripts": {
    "fetch": "bun scripts/fetch.mjs",
    "generate": "bun scripts/generate.mjs",
    "verify": "bun scripts/verify.mjs",
    "package": "bun scripts/package.mjs",
    "check": "bun scripts/check.mjs",
    "build": "bun run check && bun run generate && bun run verify && bun run package",
    "clean": "bun scripts/clean.mjs",
    "test": "bun test"
  },
  "license": "MIT"
}
